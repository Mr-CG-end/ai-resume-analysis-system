APP_VERSION=0.1.0
AI_API_KEY=
AI_BASE_URL=
AI_MODEL=
# Optional: per-attempt model request deadline in seconds (default: 20).
# AI_TIMEOUT_SECONDS=20
# CORS_ORIGINS is reserved for a later integration stage.
# Optional: leave unset to disable Redis.
# REDIS_URL=redis://localhost:6379/0
